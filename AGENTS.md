# E.O图文

## 项目简介

**E.O图文** 是一个长期维护的个人图文网站，也是 E.O 在互联网上的个人空间。

网站以原创内容为核心，坚持简洁、真实、长期积累的原则，主要展示：

- 摄影作品
- 旅行记录
- 文字作品
- AI工坊创作与实践
- 技术学习过程
- 其他值得长期保存和分享的内容

本项目源代码托管于 GitHub。正式网站由 Cloudflare Workers Static Assets 发布在 `eomoment.com`，公开媒体文件由 Cloudflare R2 通过 `media.eomoment.com` 提供。GitHub Pages 目前只保留为旧地址的过渡入口，访问者会跳转到正式域名，不再作为正式发布渠道。

---

## 网站目标

本网站不是新闻站，也不是商业网站。

它更像一本不断成长的数字笔记，一座长期建设的个人资料馆。

每一篇文章、每一张图片、每一个工具，都希望能够在多年以后仍然具有阅读和使用价值。

---

## 网站风格

整体设计遵循以下原则：

- 简洁
- 清晰
- 易读
- 内容优先
- 图片优先
- 不追求炫技
- 尽量减少无意义动画
- 优先保证手机和电脑都有良好的浏览体验

配色尽量自然、克制。

字体选择应以阅读舒适为第一目标。

界面默认使用简体中文，同时生成繁体中文版，通过首页设有的开关来切换。

---

## 内容分类

网站内容主要包括以下几个栏目：

### 摄影

摄影作品、器材体验、拍摄心得、图片故事。

### 旅行

旅行见闻、城市记录、酒店、咖啡馆、路线、地图等内容。

### 文字

读书笔记、历史、文学、艺术、人物等主题。

### AI工坊

本人开发的小工具。

包括但不限于：

- 坐标转换工具
- 摄影辅助工具
- 数据处理工具
- AI 小工具

所有工具均尽量采用纯前端实现，方便作为静态网站部署。

AI工坊同时记录与 AI 协作完成的软件、网页、图片、研究及实践经验。

重点展示人与 AI 协同工作的真实过程，而不仅是最终成果。

---

## 写作原则

所有文章应坚持：

- 内容真实
- 尽量准确
- 来源明确（需要时注明）
- 不故意夸张
- 不制造标题党
- 尊重版权
- 尊重事实

对于存在争议的话题，应尽量说明不同观点，而不是只给出单一结论。

文章使用的参考资料和核查链接应继续保存，但默认不在公开网页中显示，以免增加不必要的正式感，或让普通读者遇到无法访问的链接。

参考资料应以 HTML 注释形式保存在对应文章正文末尾，统一使用“内部参考资料（不在网页中显示）”标题，并以“资料名称：网址”的形式逐条记录，方便 Olan 日后阅读、收藏和复查。

图片授权等依法或依许可必须公开展示的信息，不应隐藏，仍需在图片说明或正文中清楚标注。

---

## 图片原则

图片应优先使用：

- 本人摄影作品
- 已获得授权图片
- 可合法使用素材

图片尽量保留良好的显示质量，同时兼顾网页加载速度。

网页展示图片应经过适当压缩，不直接使用原始照片。

### 网页图片默认处理规则

- 今后如无特别说明，网站公开图片默认生成新的响应式 `WebP` 派生文件，不覆盖原始 JPEG/TIFF/PNG 等源文件。
- 默认规格以 `640 / 960 / 1600 / 2400` 宽度为主；只生成不超过原图宽度的版本。个别较小图片、竖幅图片或专题封面，可按原图尺寸实际情况少生成一档或多档。
- 默认画质优先于极限压缩；目前公开网页图片的默认 `WebP` 质量基线为 `quality=88`，并嵌入标准 `sRGB ICC`。如个别图片出现天空断层、肤色异常或细节损失，可单独提高质量，不为了统一参数牺牲画质。
- 公开图片默认删除全部 `XMP`，删除全部 `GPS`、序列号、`MakerNotes`、软件编辑历史、原始文件名/路径、内嵌缩略图和其他无法确认用途的私有数据。
- 公开图片默认保留原片 `EXIF ImageDescription`；若原图该字段为空，则导出文件中也保持为空，便于今后统一调用。
- 对本人摄影作品和本人拥有权利的 AI 图片，公开导出文件中的作者字段统一写为 `E.O`，版权字段统一写为 `Copyright YYYY eomoment.com`。其中 `YYYY` 取该批导出图片首次用于网站公开发布的年份；新一年新导出的图片按新年份写入，不追溯改写旧批次。
- 第三方授权图片、截图、Codex 生成图片等，不默认改写其作者和版权元数据；如需处理，必须单独确认。
- 页面接入时默认使用 `srcset` / `sizes` 方式调用响应式图片，并在本地检查实际加载结果；具体操作细则见 [docs/IMAGE-PROCESSING-DEFAULTS.md](/Users/oneo/Documents/E.O图文/docs/IMAGE-PROCESSING-DEFAULTS.md)。

---

## 软件项目原则

所有工具应尽量做到：

- 无需安装
- 浏览器即可运行
- 支持桌面端
- 支持移动端
- 尽量减少第三方依赖
- 保持代码结构清晰

每个工具都应包含：

- 功能说明
- 使用方法
- 更新记录（必要时）

---

## AI 协作原则

AI 是本项目的重要协作者。

AI 的职责包括：

- 协助设计网站
- 协助编写代码
- 协助优化界面
- 协助整理文章
- 协助发现错误
- 协助生成文档
- 协助维护项目结构

但 AI 不应凭空编造事实。

涉及历史、人物、引用、数据等内容时，应尽量进行核实。

---

## 首页最新发表更新提醒

每当完成以下内容更新后，AI 应主动询问 Olan：是否把此次新增内容同步更新到首页的“最新发表”区域。

- “摄影”板块新增或更新一个专题层级内容。
- “AI工坊”新增或更新一个专题层级内容。
- “文字”板块新增一篇内容。
- “AI工坊”增加一个工具软件。

询问应在完成内容本身之后提出；未经 Olan 确认，不应自动替换首页“最新发表”。

首页“最新发表”应在标题文字后用全角括号标注对应内容的发布日期，格式为“最新发表（YYYY年M月D日）”。

“摄影”与“AI工坊”的 AI 影像专题均按新内容在前的顺序排列；新增专题应放在原有专题之前。

---

## 发布架构与流程

日常维护中，必须清楚区分“源文件”“发布副本”和“备份”：

- 本地项目与 GitHub `main` 是唯一的源文件和版本历史。所有内容、网页、样式和图片引用都先在这里修改、检查、提交和推送。
- Cloudflare Worker `eo-journal-site` 是 `eomoment.com` 的正式静态站点发布端；其 `.cloudflare-static/` 部署目录由 `scripts/prepare-cloudflare-static.sh` 临时生成且不会提交到 Git。
- Cloudflare R2 桶 `eomoment-media` 是公开媒体发布端；其 `.cloudflare-media/` 准备目录由 `scripts/prepare-cloudflare-media.sh` 临时生成且不会提交到 Git。公开图片通过 `media.eomoment.com` 提供。
- Cloudflare 中的部署副本不是源文件，也不是备份。不得直接在 Cloudflare 中临时修改网页内容；此类修改会与 GitHub 脱节，并在下一次部署时被覆盖。
- GitHub Pages 保留为旧地址跳转的过渡入口，不再承担正式内容发布或上线核验。其发布源固定为独立的 `gh-pages` 分支根目录；该分支只保留轻量 `index.html` 与 `404.html` 跳转页，绝不能改回从 `main` 发布。

需要正式发布时，按以下顺序进行：

1. 在本地修改源文件并完成相应检查。
2. 提交并推送到 GitHub `main`，确认 GitHub 已保存本次源文件与历史。
3. 如果本次新增或更换了公开图片，先准备并上传对应的 WebP（及仍被页面直接引用的少数 JPG）到 `eomoment-media`；替换图片必须使用新文件名，不能用同一路径覆盖。
4. 生成 `.cloudflare-static/` 并部署 `eo-journal-site` Worker，使网页引用与 R2 中已经可用的媒体保持一致。
5. 核验 `https://eomoment.com` 的页面、图片和控制台；图片更新还应检查实际响应和 `cf-cache-status`。当前 GitHub 推送不会自动完成 Cloudflare 部署，发布步骤必须明确执行。

## Git 工作流程

本项目会在 Mac Studio 和 MacBook 两台电脑上交替维护。GitHub 上的 `main` 分支是两台电脑之间的唯一共同基准。

每次开始工作时：

1. 先阅读 `README.md` 和 `HANDOFF.md`。
2. 检查当前分支和未提交的修改。
3. 如果工作区干净，先从 GitHub 拉取最新的 `main`，再开始修改。
4. 如果发现未提交修改、分支分叉或拉取冲突，不得直接覆盖、丢弃或强制合并，应先向 Olan 说明情况。

每次完成工作时：

1. 修改项目内容。
2. 本地检查效果。
3. 检查是否影响其他页面。
4. 如果仍有未完成且需要在另一台电脑继续的工作，更新 `HANDOFF.md`；如果已完成，将其恢复为“暂无未完成任务”。
5. 提交 Commit。
6. 推送至 GitHub，确认推送成功后才算完成本次交接。
7. 需要正式发布时，按上文“发布架构与流程”部署 Cloudflare Worker；如涉及图片，先完成 R2 上传，再检查 `eomoment.com` 的线上效果。

Commit Summary 应简洁描述本次修改内容。

不要让两台电脑同时修改同一项内容。离开当前电脑前，应尽量完成提交和推送；换到另一台电脑后，必须先拉取再修改。具体操作见 `docs/MULTI-DEVICE-WORKFLOW.md`。

---

## 网站长期发展方向

未来网站可以逐步增加：

- 全站搜索
- 标签系统
- 文章分类
- 图片画廊
- 摄影地图
- 软件作品集
- 时间轴
- 多语言支持（视需要）
- RSS
- Sitemap
- SEO 优化

所有新增功能均应保持整体风格统一，不为增加功能而增加复杂度。

---

## 长期理念

E.O图文希望成为一个能够持续维护很多年的个人网站。

它记录的不只是作品，也包括思考、学习和创造的过程。

每一次更新，都应让网站更加完整，而不是更加复杂。
